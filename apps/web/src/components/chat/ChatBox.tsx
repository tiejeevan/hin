/**
 * ChatBox — primary chat shell component.
 * Implementation currently lives in MessagesPanel; re-exported under ChatBox name
 * while callers migrate. MessagesPanel remains a compatibility alias.
 */
export { MessagesPanel as ChatBox } from '../messages/MessagesPanel';
